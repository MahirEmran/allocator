{
  "name": "allocator-dev",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "git config --global --add safe.directory /workspaces/allocator && cmake --version && g++ --version && gcovr --version && cpplint --version",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools",
        "ms-vscode.cmake-tools"
      ]
    }
  }
}
